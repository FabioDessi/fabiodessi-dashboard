extends default 

block content 
  h1 #{title}
  form(method="POST" action="/work-experiences/create")
    input(type="text" name="title")
    textarea(name="description")
    input(type="submit" value="submit")